# ------- 均可直接设置环境变量 -------
# ------- 均可直接设置环境变量 -------
# ------- 均可直接设置环境变量 -------
# 应用环境（定位yaml配置）
APP_ENV=dev
# 应用配置（指定yaml配置，优于`APP_ENV`定位）
YAML_PATH=
# api密钥
API_KEYS=
# jwt密钥
JWT_KEY=
# 雪花算法数据中心id（取值：0-31，在分布式部署时需确保每个节点的取值不同）
SNOW_DATACENTER_ID=0
